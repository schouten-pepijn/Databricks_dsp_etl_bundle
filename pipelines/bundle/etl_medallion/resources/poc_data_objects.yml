resources:
  catalogs:
    poc_databricks_catalog:
      name: poc_databricks_catalog

  schemas:
    etl_demo:
      name: etl_demo
      catalog_name: ${resources.catalogs.poc_databricks_catalog.name}

  volumes:
    raw_landing:
      name: raw_landing
      catalog_name: ${resources.catalogs.poc_databricks_catalog.name}
      schema_name: ${resources.schemas.etl_demo.name}
      volume_type: MANAGED

    checkpoints:
      name: checkpoints
      catalog_name: ${resources.catalogs.poc_databricks_catalog.name}
      schema_name: ${resources.schemas.etl_demo.name}
      volume_type: MANAGED
